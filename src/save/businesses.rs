//! Business ownership shares the implementation in [`super::properties`]
//! (`SaveManager::own_all_businesses`), since the modern save stores
//! `Businesses/<Name>.json` with the same `Objects`/`IsOwned` shape as
//! properties.
